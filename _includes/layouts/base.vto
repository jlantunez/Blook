<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="/style.css">
  </head>
  <body>
    {{ content }}

    <hr>
    <a href="/">Portada</a>
    {{ set posts = search.pages("type=post", "date=dest") }}
    {{ for post of posts }}
      <a href="{{ post.url }}">{{ post.title }}</a>
    {{ /for }}
  </body>
</html>
